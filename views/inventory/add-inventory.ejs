<h1><%- title %></h1>

<p><%- messages() %></p> 

<% if (errors) { %>
    <ul class="notice">
   <% errors.array().forEach(error => { %>
     <li><%= error.msg %></li>
  <%  }) %>
   </ul>
  <% } %>

<form action="/inv/add-inventory" method="post" class="form" id="add-inventory-form">
    <div class="form-inputs">
        <label for="classification_id">Classification:</label>
        <%- classificationList %>
        <label for="inv_make">Make:</label>
        <input type="text" name="inv_make" id="invMake" required pattern="^(?!.*\s)[a-zA-Z0-9]+$">
        <label for="inv_model">Model:</label>
        <input type="text" name="inv_model" id="invModel" required pattern="^(?!.*\s)[a-zA-Z0-9]+$">
        <label for="inv_year">Year: make the input character 4</label>
        <input type="text" name="inv_year" id="invYear" required pattern="^(?!.*\s)[a-zA-Z0-9]+$">
        <label for="inv_description">Description:</label>
        <input type="text" name="inv_description" id="invDescription" required pattern="^(?!.*\s)[a-zA-Z0-9]+$">
        <label for="inv_image">Image:</label>
        <input type="text" name="inv_image" id="invImage" required pattern="^(?!.*\s)[a-zA-Z0-9]+$">
        <label for="inv_thumbnail">Thumbnail:</label>
        <input type="text" name="inv_thumbnail" id="invThumbnail" required pattern="^(?!.*\s)[a-zA-Z0-9]+$">
        <label for="inv_price">Price:</label>
        <input type="number" name="inv_price" id="invPrice" required pattern="^(?!.*\s)[a-zA-Z0-9]+$">
        <label for="inv_miles">Miles:</label>
        <input type="number" name="inv_miles" id="invMiles" required pattern="^(?!.*\s)[a-zA-Z0-9]+$">
        <label for="inv_color">Color:</label>
        <input type="text" name="inv_color" id="invColor" required pattern="^(?!.*\s)[a-zA-Z0-9]+$">
        <div>New classification name cannot contain a space or special character of any kind.</div>
</div>
    <button type="submit">Add New Inventory</button>
</form>